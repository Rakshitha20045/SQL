/*
CREATE TABLE test(
sno INT,
name VARCHAR(20)
);

INSERT INTO test(sno,name)
VALUES(1,'RAKSHI'),
(2,'SHAN');

UPDATE test 
SET name = 'Rakshitha'
WHERE sno=1;

UPDATE test 
SET name = 'Shanmathi',sno=3
WHERE sno=2;


DELETE FROM test 
WHERE sno=3;


TRUNCATE TABLE test;

DROP TABLE test;

*/


--diff types of creating TABLE
CREATE TABLE employee(
emp_id INT PRIMARY KEY,
name VARCHAR(20) NOT NULL
);


/*
--CTAS
--MySQL
CREATE TABLE new_emp as
SELECT emp_id, name
FROM employees
WHERE name = 'rakshitha';



--SQL Server
SELECT emp_id, name
INTO employees_new
FROM employee
WHERE name = 'rakshi';

-- Temporary Table
--MYSQL
CREATE TEMPORARY TABLE temp_emp (
    emp_id INT PRIMARY KEY,
    name VARCHAR(20) NOT NULL,
    salary DECIMAL(10,2)
);

*/
--SQL Server
--Local Temporary Table (exists only for your session)
CREATE TABLE #temp_emp (
    emp_id INT PRIMARY KEY,
    name VARCHAR(20) NOT NULL,
    salary DECIMAL(10,2)
);

SELECT * FROM employee;


